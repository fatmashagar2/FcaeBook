<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:padding="16dp">

    <!-- Logo at the top -->
    <ImageView
        android:id="@+id/user_icon"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="50dp"
        android:src="@drawable/logo" />

    <!-- Space between logo and username field -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="100dp" />

    <!-- Username field -->
    <EditText
        android:id="@+id/username55"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="User Name"
        android:inputType="textEmailAddress"
        android:minHeight="48dp"
        android:padding="10dp"
        android:textColor="@android:color/black"
        android:textColorHint="@android:color/darker_gray" />


    <!-- Space between username and email field -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp" />

    <!-- Email field -->
    <EditText
        android:id="@+id/email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
         android:hint="Email"
        android:inputType="textEmailAddress"
        android:minHeight="48dp"
        android:padding="10dp"
        android:textColor="@android:color/black"
        android:textColorHint="@android:color/darker_gray" />

    <!-- Space between email and password field -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp" />

    <!-- Password field with eye icon -->
    <EditText
        android:id="@+id/pass2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:inputType="textEmailAddress"
        android:minHeight="48dp"
        android:padding="10dp"
        android:textColor="@android:color/black"
        android:textColorHint="@android:color/darker_gray" />

    <!-- Space between password and confirm password field -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="22dp" />

    <!-- Confirm Password field with eye icon -->

    <!-- Space between confirm password and register button -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp" />

    <!-- Register Button -->
    <FrameLayout
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
          android:padding="12dp"
        android:layout_marginTop="8dp">

        <TextView
            android:id="@+id/register_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="REGISTER"
               android:textAllCaps="false"
            android:layout_gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:padding="8dp" />
    </FrameLayout>

    <!-- Space between button and login link -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp" />

    <!-- Login Link -->
    <TextView
        android:id="@+id/login_link"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Already have an account? Login"
         android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp" />

</LinearLayout>
